[package]
name = "flashtool-mtk"
version = "0.1.0"
edition = "2021"

[dependencies]
flashtool-core = { path = "../flashtool-core" }
zip = { version = "2", default-features = false, features = ["deflate"] }
